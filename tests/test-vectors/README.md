# AIGC-Proof 0.2 deterministic vectors

These small UTF-8 vectors cover SHA-256, RFC 8785 JCS member ordering, and the first event hash. Files named expected contain exact bytes or lowercase hexadecimal text followed by one newline for readability. Implementations hash the canonical JSON bytes without that explanatory newline.
